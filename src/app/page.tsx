import { Badge, Button, Navbar, SSRProvider } from "react-bootstrap";
import NavbarComponent from "./components/navbar";

export const metadata = {
    title: 'Homepage',
    description: 'Homepace POC valli dell`energia pulita',
}

//Returns homepage HTML doc

export default function Home() {
    return (
        <div>
        <h1>Homepage placeholder</h1>
        </div>
    )
}